<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eldercare">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HealthMonitor.UpdateHealthActivity"></activity>
        <activity android:name=".HealthMonitor.AddHealthActivity" />
        <activity android:name=".DayPlanner.UpdateDayPlanActivity" />
        <activity android:name=".DayPlanner.AddDayPlanActivity" />
        <activity android:name=".HealthMonitor.HealthActivity" />
        <activity android:name=".MedicationActivity" />
        <activity android:name=".DayPlanner.DayPlannerActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".UserProfile.AddDetailsActivity" />
        <activity android:name=".UserProfile.ProfileActivity" />
        <activity
            android:name=".UserProfile.SignUpActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".UserProfile.LoginPage"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".UserProfile.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>